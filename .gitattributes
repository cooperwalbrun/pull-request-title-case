# These files will be normalized on commit (CRLF converted to LF) - asterisk is a wildcard
* text eol=lf

# These files will NOT be normalized
*.png -text
*.jpg -text
*.gif -text
*.ico -text

# These files will not be included in GitHub's (Linguist's) language statistics
/tests/**/mocks/* linguist-vendored
/tests/**/mocks/** linguist-vendored
